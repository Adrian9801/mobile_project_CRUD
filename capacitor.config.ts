import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile_project_CRUD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
